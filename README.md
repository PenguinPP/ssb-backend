# ssb-backend
Code for application server and database queries.

## Available api calls

/api/recipes
Returns all recipes

/api/ingredients
Returns all ingredients

/api/mainIngredients
Returns all main ingredients

/api/tags
Returns all tags
